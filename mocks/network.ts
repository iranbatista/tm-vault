import { NetworkConfig } from "@/stores/network";

export const MOCK_TARGET_NETWORK: NetworkConfig = {
  chainId: 998,
  name: "HyperEVM",
  rpcUrl: "https://rpc.hyperliquid.xyz/evm",
  isTestnet: false,
};